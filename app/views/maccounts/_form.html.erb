<%= form_with(model: maccount, local: true) do |form| %>
  <% if maccount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(maccount.errors.count, "error") %> prohibited this maccount from being saved:</h2>

      <ul>
      <% maccount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit "Create Account", class: "btn btn-outline-dark my-2 my-sm-0" %>
  </div>
<% end %>
