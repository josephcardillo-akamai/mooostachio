<%= form_with(model: mlocation, local: true) do |form| %>
  <% if mlocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mlocation.errors.count, "error") %> prohibited this mlocation from being saved:</h2>

      <ul>
      <% mlocation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit "Create Location", class: "btn btn-outline-dark my-2 my-sm-0" %>
  </div>
<% end %>
